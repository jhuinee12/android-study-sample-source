<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ss210922_webview_webserver">
    <!-- 인터넷 접속 권한 추가 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ss210922_webview_webserver"
        android:usesCleartextTraffic="true">
        <activity android:name=".KotlinWeb1Activity"></activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".Web4Activity" />
        <activity android:name=".Web3Activity" />
        <activity android:name=".Web2Activity" />
        <activity android:name=".Web1Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>